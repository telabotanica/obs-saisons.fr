{#
    #: optionnal,
    *: required,
    |: default
    [int..int]: array indexes ([...] if no precise length defined)

    id*: int
    route#: string | 'observations'
    listCard*: object
        add#: object
            text*: string (description)
            button*: string (button label)
        slug#: string | route
        image#: string (url)
        events_dates#: array of 1 (1 day), or 2 (begining - end) (route = "evenements")
            [...](date*) date
        heading*: object
            title*: string (with dropdown)
            text#: string (item-name)
        details#: object
            grey_text:# string
            infos#:# object
                bolder: string
                lighter: string
    buttons#: array (i.e. dual_squared_buttons) | (see below:
        [
            {
                'icon': 'help-circle',
                'label': 'fiche'
            },
            {
                'icon': 'edit-calendar',
                'data_attr': {
                    'action': 'open',
                    'target': 'indiv'
                },
                'label': 'saisir'
            }
        ]
    )
#}
{% set slug = listCard.slug|default(route) %}
<div {{ listCard.heading is defined and listCard.heading.title is defined? "id=#{listCard.heading.title|slugify}" }} class="list-cards-item{{ listCard.add is defined ? '-add' }}" data-id="{{ id }}">
    {% if listCard.add is defined %}
        <div class="add-list-card">
            <div class="icon-add-list-card add-square{{ route == 'evenements' ? '-orange' }}-icon"></div>
            <div class="add-card-text">{{ listCard.add.text }}</div>
            <a href="" class="button bt-add {{ route == 'evenements' ? 'orange' }}">{{ listCard.add.button }}</a>
        </div>

    {% else %}
        {% if listCard.events_dates is defined %}
            {% include "components/list-cards-components/event-dates.html.twig" with {events_dates: listCard.events_dates}%}
        {% endif %}

        {% if listCard.image is defined %}
            <div class="list-card-img {{ listCard.image == 'default' ? 'bg-default' }}" style="{{ listCard.image is defined and listCard.image != 'default' ? "background-image: url(#{listCard.image})" }}"></div>
        {% endif %}

        {% include "components/list-cards-components/heading.html.twig" with {heading: listCard.heading} %}

        {% if listCard.details is defined %}
            <div class="item-details">
                {% include "components/list-cards-components/details.html.twig" with {details: listCard.details} %}
            </div>
        {% endif %}

        {% if buttons is defined %}
            {% include "components/buttons/dual-squared-button.html.twig" with buttons %}
        {% endif %}
    {% endif %}
</div>